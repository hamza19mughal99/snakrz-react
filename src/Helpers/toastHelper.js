export function loginError(toast) {
	return toast('Something went wrong', {
		appearance: 'error',
		autoDismiss: true,

	})
}

export function inActiveVendor(toast) {
	return toast('Inactive vendor successfully', {
		appearance: 'success',
		autoDismiss: true,

	})
}

export function activeVendor(toast) {
	return toast('Active vendor successfully', {
		appearance: 'success',
		autoDismiss: true,

	})
}

export function blockVendor(toast) {
	return toast('Block vendor successfully', {
		appearance: 'success',
		autoDismiss: true,

	})
}

export function featuredVendor(toast) {
	return toast('Featured vendor successfully', {
		appearance: 'success',
		autoDismiss: true,

	})
}

export function visibleReview(toast) {
	return toast('Visible review successfully', {
		appearance: 'success',
		autoDismiss: true,

	})
}

export function hideReview(toast) {
	return toast('Hide review successfully', {
		appearance: 'success',
		autoDismiss: true,

	})
}


export function serviceEditSuccessfully(toast) {
	return toast('Service edited successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function deleted(toast) {
	return toast('Deleted Successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function created(toast) {
	return toast('Created Successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function completedAppointment(toast) {
	return toast('Completed appointment Successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function pendingAppointment(toast) {
	return toast('Pending appointment Successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function cancalledAppointment(toast) {
	return toast('Cancalled appointment Successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function deletedCategory(toast) {
	return toast('Delete category Successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function addCategory(toast) {
	return toast('Add category Successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function categoryAdded(toast) {
	return toast('Category added successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function deletedStaff(toast) {
	return toast('Delete staff successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function createdStaff(toast) {
	return toast('Created staff successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function editStaff(toast) {
	return toast('Edit staff successfully', {
		appearance: 'success',
		autoDismiss: true,
	})
}

export function confirmationForEmail(toast) {
	return toast('An email has been sent to you', {
		appearance: 'success',
		autoDismiss: true,
	})
}
